function maymin(){
	cadena=prompt("Ingrese cadena")

	if (cadena==cadena.toLowerCase())

		resultado= "Has escrito en minusculas"

	else if (cadena==cadena.toUpperCase()) 
		
		resultado= "Has escrito en minusculas"
	

	else

		resultado= "Has escrito como amixer xD"

	alert(resultado)
}